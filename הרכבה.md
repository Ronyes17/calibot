# הרכבה — מה לעשות כשמגיעים למחשב

12 קבצים מוכנים. הכל נבדק בבידוד; שום דבר עוד לא רץ מקצה לקצה מול גוגל.

## 1. איפה כל קובץ

מתוך `calibot/backend/app/`:

| קובץ | לאן | פעולה |
|---|---|---|
| `db.py` | `app/db.py` | חדש |
| `travel.py` | `app/travel.py` | חדש |
| `confirm.py` | `app/confirm.py` | חדש |
| `voice.py` | `app/voice.py` | חדש |
| `scheduler.py` | `app/scheduler.py` | חדש |
| `policy.py` | `app/policy.py` | חדש |
| `backup.py` | `app/backup.py` | חדש |
| `agent.py` | `app/agent.py` | חדש |
| `executors.py` | `app/executors.py` | חדש |
| `prompts.py` | `app/prompts.py` | **מחליף** |
| `routes.py` | `app/api/routes.py` | **מחליף** |
| `models.py` | `app/api/models.py` | **מחליף** |
| `main.py` | `app/main.py` | **מחליף** |

הישן שנשאר: `services/google_calendar.py` (אל תיגע — זה הלב),
`services/telegram.py`, `config.py`.

מה שמת: `agent/nlp_agent.py`, `services/ai_service.py`,
`services/conversation.py` — הוחלפו. אפשר למחוק אחרי שזה עובד.

היבוא ב-`routes.py` וב-`main.py` כתוב כ-`import db` וכו'.
אם ההרצה היא `python -m app.main`, שנה ל-`from app import db`.

## 2. תלויות

```bash
uv pip install apscheduler httpx
```
litellm ו-google-api-client כבר שם.

## 3. .env

```
TELEGRAM_API_TOKEN=<קיים>
GOOGLE_CREDENTIALS_FILE=<קיים>
GEMINI_API_KEY=<מבוט המסחר>

ORS_API_KEY=<להוציא ב-openrouteservice.org>
HOME_ADDRESS=גיבורי הקריה 9, קרית אתא

OWNER_CHAT_ID=<ראה למטה>
ALLOWED_CHAT_IDS=<אותו מספר>

PUBLIC_URL=https://<דומיין>
HEALTHCHECK_URL=<מ-healthchecks.io>
BACKUP_CHAT_ID=<אותו chat_id>
BACKUP_KEEP=14

QUIET_START_HOUR=23
QUIET_END_HOUR=7
```

**את ה-chat_id שלך:** שלח הודעה לבוט ואז
`curl "https://api.telegram.org/bot<TOKEN>/getUpdates"`

**אזהרה:** `ALLOWED_CHAT_IDS` ריק חוסם הכל, כולל אותך. זה מכוון.

## 4. סדר הבדיקה

1. `python -m app.main` — צריך לראות "מתזמן הופעל — 4 ג'ובים"
2. `/start` בטלגרם → תפריט העזרה
3. `/today` → אירועי היום. **זו הבדיקה האמיתית ל-OAuth ול-fetch_events**
4. "תקבע פגישה מחר ב-3 אצל הרופא בחיפה" → הצעה עם כפתורים
5. לחיצה על ✅ → האירוע מופיע ביומן בטלפון
6. `/undo` → נעלם
7. הודעה קולית → נבדק שהתמלול חוזר
8. "תקבע פגישה טלפונית מחר ב-4" → **לא אמור לשאול על מיקום**
9. שינוי זמני של `MORNING_HOUR` ב-`scheduler.py` לעוד שתי דקות → בריף בוקר

## 5. מה שכנראה יישבר בפעם הראשונה

- **הפורמט של `query_events`** — `executors._parse` מניח מבנה מסוים
  מ-`google_calendar.py`. סביר שיצטרך התאמה. הדפס את הפלט הגולמי פעם אחת.
- **`create_event`** — שמות השדות ב-`_create_event` נלקחו מהקוד הקיים,
  אבל לא הרצתי אותו. אם נופל, השווה ל-`google_calendar.py` שורה 187.
- **`event_id` בהחזרה** — `/undo` תלוי בזה שהשירות מחזיר `event_id`.
  אם לא — צריך להוציא מ-`event_link`.
- **tool calling ב-Gemini דרך litellm** — אם השמות של השדות שונים,
  השגיאה תהיה ב-`agent.handle_message` בגישה ל-`tool_calls`.

## 6. systemd

```ini
[Unit]
Description=Personal assistant bot
After=network.target

[Service]
WorkingDirectory=/opt/assistant/backend
EnvironmentFile=/opt/assistant/.env
ExecStart=/opt/assistant/.venv/bin/python -m app.main
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

## 7. מה שעוד לא נכתב

מתוך 14 השיפורים: undo ופקודות מהירות ורשימת היתר ודופק ושבת — נכנסו.
עדיין לא: snooze לנדנוד, שיבוץ משימות ליומן, חיפוש חלונות פנויים,
קליטה מ-Gmail, סיכום שבועי, למידת דפוסים, גיבוי DB, תשובות קוליות.

כדאי לבנות אותם אחרי שבוע שימוש, לא לפניו.
